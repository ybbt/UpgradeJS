Создай класс ToDoList который можно использовать в коде так:

    new ToDoList();

и который сразу создаст базовую разметку.

Класс должен уметь (читай - содержать методы) добавлять и удалять задачи.

На данном этапе не имеет значение как хранятся данные, кто (какой метод) работает с DOM или записан метод во внешний или внутренний интерфейс.



ДОП:

1. Дай возможность указывать контейнер для создания базовой разметки (сейчас указан document.body но например я захочу это сделать внутри другого элемента скажем document.querySelector('._listInsider'))

2. Создай 2 списка, они должны работать независимо друг от друга.